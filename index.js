const numbers = require("./numbers")
const {
    toInt,
    zero,
    successor,
    one,
    two,
    three,
    four,
    add,
    multiply,
    exponent
} = numbers

setInterval(() => {}, 1 << 30)
